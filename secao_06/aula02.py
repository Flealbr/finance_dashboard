"""
import matplotlib.pyplot as plt
import numpy as np

Criando o Gráfico:
plt.barh(varios parâmetros): 2 parâmtros obrigatórios
    - y (iterável): conte'm as posições de onde crescerão as barra horizontais
    - tamanho (iterável): contémm os tamanhos das barra.
Parâmetros opcionais:
    - height (altura): Parâmetro que altera a altura das barras (equivalante a largura da coluna)
    - align (alinhamento): parâmetro que determina o alinhamento das barras (recebe uma str)
    - label: Coloca uma legenda no gráfico.

plt.ylabel(str): altera a legenda do eixo y

plt.yticks(y, legendas): determina em quais posições do eixo y serão colocadas as legendas

plt.title(str): coloca um título no gráfico
plt.legend(): imprimi a legenda
plt.show(): imprimi o gráfico na tela.


"""
